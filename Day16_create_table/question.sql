Q1 — Students ⭐

Create a table called Students with:

StudentID → integer, primary key
StudentName → varchar(50)
Age → integer
Course → varchar(50)

Then insert 5 students.

Q2 — Products ⭐

Create a table called Products2 with:

ProductID → integer, primary key
ProductName → varchar(50)
Category → varchar(30)
Price → decimal(10,2)

Then insert 5 products from different categories.

Q3 — Employees ⭐⭐

Create a table called Employees2 with:

EmployeeID → integer, primary key
FirstName → varchar(50)
LastName → varchar(50)
Salary → decimal(10,2)
Department → varchar(50)

Then insert 5 employees with different salaries and departments.

Q4 — Customers ⭐⭐

Create a table called Customers2 with:

CustomerID → integer, primary key
CustomerName → varchar(100)
Email → varchar(100)
City → varchar(50)
Age → integer

Then insert 5 customers from different cities.

Q5 — Orders ⭐⭐⭐

Create a table called Orders2 with:

OrderID → integer, primary key
CustomerID → integer
ProductName → varchar(50)
Quantity → integer
OrderDate → date
TotalAmount → decimal(10,2)