full outer join is time consuming beacuse it inlcude vaalue from inner, left tbale, right table and the size of data also increase 
if it is possbile to get the result using different query than it is good to avoid it else full outer join generally take alot of time.


self join- joininig a single table with it own self.


note : full outer join work different with respect to database

eg : using mysql workbench in this we have to apply full outer little different - left table (union) right  then we get the value = 


